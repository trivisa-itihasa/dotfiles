# ~/.config/starship.toml または ~/dotfiles/.config/starship.toml

# プロンプト全体の設定
format = """
$username\
$hostname\
$directory\
$git_branch\
$git_status\
$nodejs\
$python\
$rust\
$java\
$docker_context\
$kubernetes\
$cmd_duration\
$line_break\
$character"""

palette = "monokai_pro"

# カラーパレット
[palettes.monokai_pro]
color_dark = "#19181a"
color_black = "#221f22"
color_red = "#ff6188"
color_green = "#a9dc76"
color_yellow = "#ffd866"
color_orange = "#fc9867"
color_purple = "#ab9df2"
color_cyan = "#78dce8"
color_white = "#fcfcfa"

# 文字設定
[character]
success_symbol = "[>](bold color_green)"
error_symbol = "[>](bold color_red)"
vicmd_symbol = "[V](bold color_green)"

# ディレクトリ表示
[directory]
style = "bold color_green"
truncation_length = 0
truncation_symbol = "…/"
home_symbol = "~"

# Git ブランチ
[git_branch]
symbol = " "
style = "bold color_purple"
truncation_length = 20
truncation_symbol = "…"

# Git ステータス
[git_status]
style = "bold color_red"
ahead = "↑${count}"
diverged = "↕↑${ahead_count}↓${behind_count}"
behind = "↓${count}"
conflicted = "CONFLICT"
untracked = "UNTRACKED"
stashed = "STASHED"
modified = "MODIFIED"
staged = "STAGED(${count})"
renamed = "RENAMED"
deleted = "DELETED"

# ユーザー名
[username]
style_user = "bold color_yellow"
style_root = "bold color_red"
format = "[$user ]($style)"
disabled = false
show_always = true

# ホスト名
[hostname]
ssh_only = false
format = "on [$hostname](bold color_orange) "
disabled = false

# 実行時間
[cmd_duration]
min_time = 2_000
format = "took [$duration](bold color_yellow)"

# プログラミング言語固有設定
[nodejs]
symbol = "⬢ "
style = "bold color_green"

[python]
symbol = "🐍 "
style = "bold color_yellow"

[rust]
symbol = "🦀 "
style = "bold color_red"

[java]
symbol = "☕ "
style = "bold color_red"

# Docker
[docker_context]
symbol = "🐳 "
style = "bold color_cyan"

# Kubernetes
[kubernetes]
format = 'on [⛵ $context \($namespace\)](bold color_green) '
disabled = false
